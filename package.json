{
  "name": "mercurius",
  "version": "1.0.0",
  "description": "Chrome extension for collaborative web commenting",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}